import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  brand: {
    flexGrow: 1,
    textDecoration: "none",
    color: "inherit",
  },
  profile: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
}));
